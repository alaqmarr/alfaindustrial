import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Alfa Industrial Products | Valves, Fittings & Pneumatic Components",
  description: "Alfa Industrial Products is an Indian manufacturer, supplier, and distributor of industrial valves, pipe fittings, and pneumatic components based in Secunderabad, Telangana. Serving excellence for over 20 years.",
  keywords: ["industrial valves", "pipe fittings", "pneumatic components", "solenoid valves", "ball valves", "Hyderabad", "Secunderabad", "Telangana"],
  icons: {
    icon: '/favicon.png',
    apple: '/favicon.png',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <div className="flex flex-col min-h-screen">
          <Header />
          <main className="flex-grow">{children}</main>
          <Footer />
        </div>
      </body>
    </html>
  );
}
